package de.MarkusTieger.tigerclient.cosmetics.render;

public class CrownRenderer/*
							 * extends LayerRenderer<AbstractClientPlayerEntity,
							 * PlayerModel<AbstractClientPlayerEntity>>
							 */ {

	/*
	 * private final CrownModel<AbstractClientPlayerEntity> model; private final
	 * ResourceLocation loc = new ResourceLocation(Client.getInstance().getModId(),
	 * "textures/cosmetics/hats/crown/crown.png"); private final ICosmeticManager
	 * cosmetic;
	 * 
	 * public CrownRenderer( IEntityRenderer<AbstractClientPlayerEntity,
	 * PlayerModel<AbstractClientPlayerEntity>> entityRendererIn) {
	 * super(entityRendererIn); model = new
	 * CrownModel<AbstractClientPlayerEntity>(getEntityModel()); cosmetic =
	 * Client.getInstance().getInstanceRegistry().load(ICosmeticManager.class); }
	 * 
	 * @Override public void render(PoseStack PoseStackIn, IRenderTypeBuffer
	 * bufferIn, int packedLightIn, AbstractClientPlayerEntity entitylivingbaseIn,
	 * float limbSwing, float limbSwingAmount, float partialTicks, float ageInTicks,
	 * float netHeadYaw, float headPitch) {
	 * 
	 * boolean render = cosmetic.hasHat(entitylivingbaseIn, "crown"); // render =
	 * true; // debug
	 * 
	 * if (entitylivingbaseIn.hasPlayerInfo() && !entitylivingbaseIn.isInvisible()
	 * && render) { IVertexBuilder vertex =
	 * bufferIn.getBuffer(RenderType.getEntitySolid(loc)); model.render(PoseStackIn,
	 * vertex, packedLightIn, packedLightIn, packedLightIn, ageInTicks, netHeadYaw,
	 * headPitch); }
	 * 
	 * }
	 */

}
